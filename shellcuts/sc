# ADD DOCUMENTATION

OUTCOME=$(sc-init)
F_BASHRC="~/.bashrc"

if [ "${OUTCOME:0:5}" == "PRINT" ]; then
    echo "${OUTCOME:6:${#OUTCOME}}"
elif [ "${OUTCOME:0:6}" == "SOURCE" ]; then
    echo "${OUTCOME:7:${#OUTCOME}}"
    . $F_BASHRC
fi
